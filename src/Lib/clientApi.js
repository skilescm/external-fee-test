import { getFeed, searchFeed } from './Endpoints/feedEndpoint';

//used to combine all of the different endpoints within different endpoint files, put here to support future expansion of the app.

export default { getFeed, searchFeed };